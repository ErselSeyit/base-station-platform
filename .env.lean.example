# Lean Stack Environment Variables
# Copy to .env and update with secure values
#
# REQUIRED - These must be set (no defaults for security)

# PostgreSQL (single instance with multiple databases)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=<generate-strong-password>

# MongoDB
MONGODB_USER=admin
MONGODB_PASSWORD=<generate-strong-password>

# RabbitMQ
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=<generate-strong-password>

# Redis
REDIS_PASSWORD=<generate-strong-password>

# HMAC Secret (used for service-to-service authentication)
# Generate: openssl rand -hex 32
HMAC_SECRET=<generate-64-char-hex-string>

# Grafana
GRAFANA_PASSWORD=<generate-strong-password>

# To generate secure passwords:
#   openssl rand -base64 24
#
# To generate HMAC secret:
#   openssl rand -hex 32
